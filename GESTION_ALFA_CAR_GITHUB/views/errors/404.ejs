<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Non Trouvée | ALFA CAR</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        *{margin:0;padding:0;box-sizing:border-box}
        body{font-family:'Inter',sans-serif;min-height:100vh;display:flex;align-items:center;justify-content:center;background:linear-gradient(135deg,#0a0a1a 0%,#1a1a2e 50%,#16213e 100%);color:white;text-align:center;overflow:hidden}
        .error-container{position:relative;z-index:1;padding:40px}
        .error-code{font-size:clamp(100px,20vw,200px);font-weight:800;background:linear-gradient(135deg,#e94560,#ff6b81);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;line-height:1;margin-bottom:10px}
        .error-title{font-size:2rem;font-weight:600;margin-bottom:15px}
        .error-text{font-size:1.1rem;opacity:0.7;margin-bottom:40px;max-width:500px;margin-left:auto;margin-right:auto}
        .error-icon{font-size:4rem;margin-bottom:20px;opacity:0.3}
        .btn-home{display:inline-flex;align-items:center;gap:10px;padding:14px 32px;background:linear-gradient(135deg,#e94560,#ff6b81);color:white;text-decoration:none;border-radius:12px;font-weight:600;font-size:1rem;transition:transform 0.3s,box-shadow 0.3s}
        .btn-home:hover{transform:translateY(-3px);box-shadow:0 10px 30px rgba(233,69,96,0.4);color:white}
        .btn-secondary-link{display:inline-flex;align-items:center;gap:8px;margin-left:15px;padding:14px 32px;border:2px solid rgba(255,255,255,0.3);color:white;text-decoration:none;border-radius:12px;font-weight:600;transition:all 0.3s}
        .btn-secondary-link:hover{border-color:#e94560;color:#e94560}
        .floating-shapes div{position:absolute;border-radius:50%;opacity:0.05;background:white}
        .floating-shapes .s1{width:300px;height:300px;top:-100px;left:-100px;animation:float 8s infinite}
        .floating-shapes .s2{width:200px;height:200px;bottom:-50px;right:-80px;animation:float 6s infinite reverse}
        @keyframes float{0%,100%{transform:translateY(0) rotate(0)}50%{transform:translateY(-20px) rotate(5deg)}}
    </style>
</head>
<body>
    <div class="floating-shapes"><div class="s1"></div><div class="s2"></div></div>
    <div class="error-container">
        <div class="error-icon"><i class="fas fa-car-crash"></i></div>
        <div class="error-code">404</div>
        <h1 class="error-title">Page Non Trouvée</h1>
        <p class="error-text">La page que vous cherchez n'existe pas ou a été déplacée. Vérifiez l'URL ou retournez à l'accueil.</p>
        <div>
            <a href="/" class="btn-home"><i class="fas fa-home"></i>Retour à l'accueil</a>
            <a href="/cars" class="btn-secondary-link"><i class="fas fa-car"></i>Nos Voitures</a>
        </div>
    </div>
</body>
</html>
